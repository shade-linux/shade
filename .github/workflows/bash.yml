name: Bash

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install bash
        run: |
          sudo ./src/shade setup
          yes | sudo ./src/shade install bash
